function getValue(elemetnId){
  let textfieldData = document.getElementById(elemetnId);
    return textfieldData.value;
}
function calculateValue(elementId){
    if(elementId === "triClc"){
       return 0.5 * getValue(id) * getValue()
    }
}

